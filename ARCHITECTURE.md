# المعمارية — نظام المبيعات الآلي لشركة الفنادق

---

## ١. المبدأ الحاكم

> **نموذج اللغة طبقة محادثة فقط. لا يحسب سعراً، ولا يحجز غرفة، ولا يرى تكلفة.**

كل قرار يتعلق بالمال أو المخزون يُتخذ في خدمات حتمية مكتوبة بكود عادي، قابلة للاختبار، ونتيجتها ثابتة لنفس المدخلات.

النموذج يستدعي هذه الخدمات كأدوات، ويستقبل منها خيارات محدودة، ويتكلم بها فقط. وأي رقم ينطق به يُتحقق منه في السيرفر قبل التنفيذ.

**السبب:** حقن التعليمات هو الخطر الأول في وكيل يبيع للعامة. الرد الوحيد الفعّال هو ألا يملك النموذج الصلاحية أصلاً — لا أن نطلب منه ألا يستخدمها.

---

## ٢. المكوّنات

```
┌─────────────────────────────────────────────────┐
│  WhatsApp Cloud API  (رقم تجاري واحد)            │
└───────────────┬─────────────────────────────────┘
                │ webhook (موقّع)
┌───────────────▼─────────────────────────────────┐
│  agent-service        (FastAPI)                  │
│  • تحقق التوقيع + idempotency                    │
│  • حالة المحادثة                                 │
│  • استدعاء Gemini + tools                        │
│  • حارس المخرجات (output guard)                  │
└──┬──────────┬──────────┬───────────────┬─────────┘
   │          │          │               │
┌──▼────┐ ┌───▼─────┐ ┌──▼────────┐ ┌───▼────────┐
│pricing│ │inventory│ │ booking   │ │ escalation │
│service│ │ service │ │ service   │ │  service   │
└──┬────┘ └───┬─────┘ └──┬────────┘ └───┬────────┘
   └──────────┴──────────┴──────────────┘
                    │
        ┌───────────▼────────────┐
        │  Supabase / Postgres   │
        │  RLS على كل جدول        │
        └───────────┬────────────┘
                    │
   ┌────────────────┴─────────────────┐
   │                                   │
┌──▼──────────────┐          ┌────────▼─────────┐
│ admin-dashboard │          │  worker (cron)   │
│    (Next.js)    │          │ • إفراج المهل     │
└─────────────────┘          │ • التنبيهات       │
                             │ • تمرير المحاسبة  │
                             └──────────────────┘
```

---

## ٣. التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| قاعدة البيانات | Supabase / Postgres | RLS أصلية، معاملات قوية، خبرة سابقة |
| الخدمات | FastAPI (Python) | خبرة سابقة، اختبارات سهلة |
| الواجهة الإدارية | Next.js | خبرة سابقة |
| النموذج | Gemini | قرار معتمد — خلف واجهة قابلة للاستبدال |
| قناة المحادثة | WhatsApp Cloud API الرسمي | رقم تجاري واحد معتمد |
| المهام المجدولة | worker منفصل + pg_cron | إفراج المهل والتنبيهات |
| المستودع | GitHub | نسخ، مراجعة، تتبع |

### الاستضافة

| المكوّن | الاستضافة |
|---|---|
| قاعدة البيانات | Supabase (أقرب منطقة متاحة) |
| خدمات FastAPI + worker | Railway (أو Fly.io / Render) |
| الواجهة الإدارية | Vercel أو Railway |

**نقطة مفتوحة:** هل يشترط العميل بقاء البيانات داخل المملكة؟ إن نعم، يلزم استضافة ذاتية أو مزوّد محلي، ويتغير تقدير التكلفة والجهد.

### ما لا نستخدمه ولماذا
- **n8n**: للتنبيهات والتكاملات الجانبية فقط. **ممنوع** أن يحتوي أي منطق تسعير أو حجز — لأنه غير قابل للاختبار الآلي ولا للمراجعة في Git بشكل موثوق.
- **Redis**: غير ضروري بهذا الحجم. Postgres يكفي للأقفال والطوابير، وكل مكوّن إضافي نقطة فشل جديدة.

---

## ٤. نموذج البيانات

### الجداول الأساسية

**hotels** — الفنادق
**room_types** — أنواع الغرف لكل فندق

**seasons** — المواسم — **يعرّفها العميل بالكامل من التقويم**
`name` · `calendar_type` (hijri | gregorian) · `start_month` · `start_day` · `end_month` · `end_day` · `priority` · `is_default`

**القواعد:**
1. البداية داخل، النهاية خارج.
2. عند التداخل يفوز الأعلى `priority` — والعميل هو من يحدد الأولوية. النظام لا يفترض أن الهجري أهم من الميلادي أو العكس.
3. **موسم افتراضي واحد إلزامي** (`is_default = true`) يغطي السنة كاملة بأولوية صفر. أي تاريخ لا يقع في موسم معرّف يرجع إليه.

**الأثر:** لا يمكن أن يوجد تاريخ بلا سعر. الفجوات في تعريف العميل تُغطى تلقائياً بالافتراضي بدل أن يفشل التسعير. هذا يزيل الحاجة لحسم الفجوات قبل الإطلاق.

**allotments** — الحصة المخصصة
`hotel_id` · `room_type_id` · `date` · `total_rooms` · `cost_per_night`

**room_night_inventory** — الوحدة الذرية
`allotment_id` · `date` · `total` · `reserved` · `held`
```sql
CONSTRAINT inventory_never_oversold
  CHECK (reserved + held <= total AND reserved >= 0 AND held >= 0)
```
هذا القيد هو خط الدفاع الأخير ضد البيع المزدوج. لا يُزال ولا يُعطّل.

**price_rules** — قواعد التسعير
`scope` (global | season | hotel | room_type) · `scope_id` · `target_margin` · `min_profit_by_lead_time` (jsonb) · `demand_curve`

**quotes** — سجل غير قابل للتعديل
كل سعر نُطق به: التاريخ، رقم العميل، الليالي، سعر العرض، الحد الأدنى المسموح، القاعدة المنتِجة، معرّف المحادثة.
`INSERT` فقط — لا `UPDATE` ولا `DELETE`.

**holds** — الحجوزات المؤقتة
`expires_at` · `released_at` — يفرجها الـ worker تلقائياً

**bookings** — الحجوزات المؤكدة
`status` · `payment_status` · `accounting_sync_status`

**conversations** / **messages** — سجل المحادثات
**escalations** — حالات التصعيد وزمن الاستجابة
**audit_log** — كل تغيير في القيم المالية: من، متى، القيمة قبل وبعد

---

## ٥. خدمة التسعير

### الواجهة
```
POST /pricing/quote
IN : hotel_id, room_type_id, check_in, check_out, rooms
OUT: {
  ask_price_total,        # سعر العرض
  min_allowed_total,      # أدنى سعر مسموح به
  nights: [ {date, season_id, ask, min_allowed} ],
  negotiation_open: bool,
  quote_id
}
```

### قواعد صارمة
1. **لا تُرجع `cost` أبداً.** غير موجودة في الاستجابة، ولا في سياق النموذج.
2. التسعير **لكل ليلة** ثم يُجمع. الإقامة التي تعبر موسمين تُسعّر بموسمين.
3. `negotiation_open` تُحسب من المدة المتبقية للوصول فقط. خارج النافذة = `false`، والنموذج لا يملك أي مساحة.
4. الطلب يحرّك `ask_price` فقط. `min_allowed` لا يتأثر بالطلب إطلاقاً.
5. كل استدعاء يُسجَّل في `quotes` قبل الرد.

### تسلسل الحساب
```
cost
  → + target_margin (من قواعد التوريث: عام ← موسم ← فندق ← نوع غرفة)
  → × معامل الطلب (نسبة التصريف × المدة المتبقية)
  → = ask_price

cost
  → + min_profit (حسب المدة المتبقية والموسم)
  → = min_allowed

تجاوز يدوي إن وُجد ← يتقدم على الاثنين، لمدة محددة
```

---

## ٦. خدمة المخزون والحجز

### الحجز المؤقت
```
BEGIN;
  SELECT * FROM room_night_inventory
    WHERE allotment_id = ANY(...) AND date BETWEEN ...
    FOR UPDATE;                          -- قفل الصفوف

  -- التحقق من التوفر لكل ليلة
  UPDATE room_night_inventory SET held = held + :rooms ...;
  INSERT INTO holds (...) RETURNING id;
COMMIT;
```
لو انتهك القيد، تفشل المعاملة كاملة. **لا يوجد تحقق في طبقة التطبيق يُعتمد عليه وحده.**

### مدة المهلة
| قبل الوصول | المهلة |
|---|---|
| > ١٥ يوم | ٤٨ ساعة |
| ٣ – ١٥ يوم | ١٢ ساعة |
| < ٤٨ ساعة | ساعتان + دفع كامل |

### التأكيد
`held → reserved` في معاملة واحدة، بعد تأكيد الدفع فقط.
**الكاش عند الوصول لا يؤكده الوكيل** — يفتح تصعيداً بشرياً.

### الإفراج
worker يعمل كل دقيقة: كل `hold` منتهٍ → `held -= rooms`, `released_at = now()`.

---

## ٧. طبقة الوكيل

### الأدوات المتاحة للنموذج
| الأداة | ما تفعله |
|---|---|
| `check_availability` | التوفر لتواريخ محددة |
| `get_quote` | سعر العرض والحد الأدنى المسموح |
| `create_hold` | حجز مؤقت |
| `confirm_booking` | تأكيد بعد الدفع |
| `escalate` | تحويل لبشر |
| `search_alternatives` | فنادق بديلة بنفس التواريخ |

**النموذج لا يملك أي أداة كتابة مباشرة على قاعدة البيانات.**

### حارس المخرجات
قبل إرسال أي رسالة للعميل، يمر النص على فحص برمجي:
1. استخراج أي أرقام مالية في النص
2. مطابقتها مع `quote` نشط في هذه المحادثة
3. رقم غير مطابق أو أقل من `min_allowed` → **الرسالة تُحجب**، ويُفتح تصعيد، ويُسجَّل الحادث

هذه آخر طبقة دفاع. تعمل حتى لو انهار النموذج تماماً.

### حدود التفاوض
- خارج النافذة: `negotiation_open = false` → صفر تنازل
- داخل النافذة: ثلاث تنازلات كحد أقصى، بخطوات متناقصة (½ ثم ⅓ ثم الباقي من المساحة)
- كل تنازل مشروط بمقابل
- عدّاد التنازلات يُحفظ في حالة المحادثة في قاعدة البيانات، **لا في سياق النموذج**

---

## ٨. الأمان

| الطبقة | الإجراء |
|---|---|
| القناة | تحقق توقيع Meta على كل webhook، رفض غير الموقّع |
| التكرار | `message_id` فريد — رسالة مكررة تُتجاهل |
| المعدل | سقف رسائل لكل رقم يومياً، وسقف إنفاق على الـ API |
| الصلاحيات | RLS على كل جدول، رفض افتراضي |
| التكلفة | سياسة منفصلة — مستخدم مخوّل واحد فقط |
| المفاتيح | `service_role` على السيرفر فقط، لا يصل للمتصفح ولا لـ n8n |
| الأسرار | خزنة/متغيرات بيئة، لا شيء في المستودع |
| الإدارة | تحقق ثنائي إلزامي، حسابات فردية |
| التدقيق | `audit_log` لكل تغيير مالي، `quotes` غير قابل للتعديل |
| النسخ | يومي، **مع اختبار استرجاع شهري موثّق** |

---

## ٩. التكامل مع أنظمة العميل

| النظام | الاتجاه | الحالة |
|---|---|---|
| AMR (المخزون) | العميل يدخل الحصة عندنا يدوياً | **الافتراضي** — لا يعتمد على API |
| AMR (لو توفر API) | قراءة الحصة آلياً | تحسين لاحق، لا يعطّل الإطلاق |
| النظام المحاسبي | دفع بيانات الحجز | يحتاج تأكيد الاسم وطريقة الربط |

**قاعدة:** لا يعتمد أي مسار حرج على نظام خارجي غير مؤكد. التكامل تحسين، لا شرط إطلاق.
